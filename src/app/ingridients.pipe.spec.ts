import { IngridientsPipe } from './ingridients.pipe';

describe('IngridientsPipe', () => {
  it('create an instance', () => {
    const pipe = new IngridientsPipe();
    expect(pipe).toBeTruthy();
  });
});
